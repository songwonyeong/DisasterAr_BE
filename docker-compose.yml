version: "3.8"

services:
  backend:
    build: .
    container_name: disaster-backend
    ports:
      - "${SERVER_PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      # file.upload.dir=uploads/maps 를 로컬과 연결
      - ./uploads/maps:/app/uploads/maps

